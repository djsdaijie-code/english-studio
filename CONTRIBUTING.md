# Contributing to English Studio

Use Python 3.14 and create an isolated data directory for development. Run the full test suite before opening a change. Never add API keys, user databases, recordings, generated audio, logs or screenshots containing personal data to Git.

Keep product changes small and covered by tests. Database migrations must have a rollback test and must preserve earlier schema data.
